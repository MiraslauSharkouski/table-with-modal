## 9. Финализация и деплой

- [ ] **Code Review чеклист**
  - [ ] Нет `any`, все типы выведены или явно указаны
  - [ ] Обработаны `null`/`undefined` в цепочках доступа к данным
  - [ ] Нет утечек: очистка `setTimeout` в хуках, `AbortController` для запросов
  - [ ] Код отформатирован через `prettier` / `biome`, линтинг без ошибок

- [ ] **Документация и DX**
  - [ ] JSDoc для публичных хуков: `@param`, `@returns`, `@example`
  - [ ] Storybook (опционально): изолированные истории для `EntityTable`, `EntityFormModal`
  - [ ] `README.md`: как запустить, как добавить новую колонку, как изменить валидацию

- [ ] **Подготовка к масштабированию**
  - [ ] Вынести конфигурацию колонок в отдельный файл для переиспользования
  - [ ] Заложить возможность пагинации: `pagination: { pageSize: 10 }`
  - [ ] Подготовить хук `useEntitySync` для будущей интеграции с API (React Query / Server Actions)

- [ ] **Деплой на Vite**
  - [ ] `npm run build` → проверка вывода `dist/`
  - [ ] Предпросмотр: `npm run preview`
  - [ ] Настройка `vite.config.ts` для `base` path при деплое на subdirectory

---
