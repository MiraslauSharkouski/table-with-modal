## 5. Глобальный поиск с debounce

- [ ] **Компонент SearchBar**
  - [ ] Использовать `Input` от AntD с `allowClear`, `placeholder`, иконкой
  - [ ] Реализовать debounce через кастомный хук (без lodash, если не нужен):
    ```ts
    // useDebouncedSearch.ts
    export function useDebouncedSearch<T>(
      value: T,
      delay: number = 300,
    ): [T, (v: T) => void] {
      const [debounced, setDebounced] = useState(value);
      useEffect(() => {
        const id = setTimeout(() => setDebounced(value), delay);
        return () => clearTimeout(id);
      }, [value, delay]);
      return [debounced, setValue];
    }
    ```
  - [ ] Поддержка `onClear` → сброс состояния в родительском компоненте

- [ ] **Логика фильтрации**
  - [ ] Функция `matchesSearch(entity: EntityItem, query: string): boolean`:
    ```ts
    const q = query.toLowerCase().trim();
    return (
      entity.name.toLowerCase().includes(q) ||
      dayjs(entity.date).format("DD.MM.YYYY").includes(q) ||
      entity.value.toString().includes(q)
    );
    ```
  - [ ] Если нужен сложный поиск — подключить `lodash-es` с tree-shaking:
    ```ts
    import { escapeRegExp } from "lodash-es"; // только нужная функция
    ```

- [ ] **Интеграция с таблицей**
  - [ ] Порядок обработки: `rawData → filter by search → sort → render`
  - [ ] Фильтрация и сортировка применяются к `memoized` данным (Compiler оптимизирует)
  - [ ] Опционально: сохранять `searchQuery` в URL через `useSearchParams` (Vite + React Router)

---
